gtfs_project:
  outputs:
    dev:
      account: "${DBT_ACCOUNT}"
      database: "${DBT_DATABASE}"
      password: "${DBT_PASSWORD}"
      role: "${DBT_ROLE}"
      schema: "${DBT_SCHEMA}"
      threads: 1
      type: snowflake
      user: "${DBT_USER}"
      warehouse: "${DBT_WAREHOUSE}"
  target: dev